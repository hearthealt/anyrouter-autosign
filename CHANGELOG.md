# 更新日志

## 0.0.4

### 版本检查与更新可靠性

+ [新增] 提示新版本前先确认镜像仓库中确实存在该版本的镜像，不再提示一个 Watchtower 拉不到的版本
+ [新增] 校验移动标签 latest 是否已指向新版本镜像，latest 仍指向旧镜像时不再显示可更新
+ [修复] 手工创建 GitHub Release 或推送 v* tag 未触发发布工作流时，更新不再静默失败并误报成功
+ [优化] 镜像校验仅在发现更高版本时执行，同版本不再产生多余的镜像仓库请求
+ [优化] 镜像仓库网络异常、鉴权失败或返回 5xx 时一律放行，避免临时故障隐藏真实可用的更新
+ [优化] 触发更新接口复用同一校验，后端二次确认时同样拦截镜像缺失的版本
+ [新增] 新增 VERIFY_UPDATE_IMAGE、UPDATE_REGISTRY 和 UPDATE_IMAGE_REPO 配置项，可关闭校验或指向私有镜像仓库

## 0.0.3

### 更新流程与稳定性

+ [重构] 更新并重启改为“更新任务 + 健康检查”流程，不再依赖固定等待时间判断更新结果
+ [修复] 更新触发请求改为后台通知 Watchtower，避免容器重启时响应被 Cloudflare 502 截断后误报更新失败
+ [新增] 前端生成并保存 update_id，支持在更新期间刷新页面后继续查询原更新任务
+ [新增] 后端将更新状态原子写入 data 挂载目录，应用容器重建后仍可继续确认更新进度
+ [新增] 增加系统健康检查和更新状态接口，返回当前运行版本、目标版本、任务状态和等待时长
+ [优化] 前端每 3 秒轮询服务健康状态，502、503、连接中断和请求超时视为重启期间的临时状态并继续等待
+ [修复] 只有确认新容器健康且运行版本达到目标版本后才自动刷新页面，避免更新尚未完成时提前刷新
+ [优化] 更新确认成功后仅延迟 800 毫秒刷新页面，超时后保留手动刷新入口
+ [修复] 全局版本弹窗和设置页更新流程共用同一控制器，避免重复轮询和重复刷新

## 0.0.2

### 签到性能与稳定性

+ [优化] 批量签到、定时自动签到及失败重试改为受限并发执行，默认并发数为 8，大幅缩短多账号签到等待时间
+ [优化] 每个并发签到任务使用独立数据库会话，避免在线程之间共享 Session 导致事务和连接状态冲突
+ [优化] 前端批量签到、启用、禁用、健康检查及删除等账号操作采用 8 路并发处理，并汇总成功、失败和跳过数量
+ [优化] 签到后缓存刷新失败不再覆盖已经取得的签到结果，确保真实签到状态可以正常返回和记录
+ [优化] 定时签到及重试任务统一复用并发签到服务，同时保留签到日志、奖励统计、实时事件和汇总通知
+ [修复] 并发签到期间账号被禁用、删除或缺少平台配置时安全跳过，避免写入失效外键日志
+ [优化] SQLite 连接等待超时调整为 30 秒，降低并发写入期间出现数据库锁错误的概率

### 账号状态与统计

+ [优化] 账号状态统一为“正常、异常、禁用”三种互斥状态，禁用状态优先于历史健康检查结果
+ [修复] 总览异常账号统计排除已禁用账号，解决禁用账号仍被计入异常数量的问题
+ [新增] 总览接口增加正常账号数和禁用账号数，账号列表摘要增加正常账号数
+ [新增] 账号列表支持按正常状态筛选，并保留旧健康状态参数及字段以兼容已有客户端
+ [优化] 总览、账号列表和数据统计页面统一使用相同的账号状态标签、筛选条件和统计口径
+ [修复] 禁用账号不再显示异常行样式，也不再触发低额度告警样式

### 前端布局与交互

+ [优化] 总览、账号、平台、签到日志、数据统计和系统设置页面改为紧凑型工作区工具栏，移除占用空间较大的标题说明区域
+ [优化] 缩小侧边栏、顶部导航、页面间距和卡片留白，提高桌面端信息密度和操作效率
+ [优化] 筛选条件、状态统计和页面操作统一为紧凑布局，并补充移动端自动换行和横向滚动适配
+ [修复] 主内容区域增加可收缩宽度约束，解决侧边栏展开时页面被表格撑宽、必须左右滑动才能查看完整内容的问题
+ [优化] 顶部导航搜索区域使用可收缩网格，较窄窗口下不再挤压页面整体宽度

## 0.0.1

### 主要功能

+ [新增] 多平台账号管理，可分别配置平台地址、签到方式及用户、模型、分组、Token、状态、控制台和验证码等接口
+ [新增] 账号批量导入、本地分组、启用/禁用、批量迁移平台及账号级 HTTP/HTTPS 自定义代理
+ [新增] Session Cookie 与账号密码登录，支持登录即签到、Cookie 自动刷新和失效后重新登录
+ [新增] 单账号、批量及定时自动签到，支持失败重试、签到日志和签到后额度自动同步
+ [新增] 图片验证码签到，内置 OCR、多候选识别和算式验证码处理
+ [新增] 账号健康检查、定时健康告警、额度与请求统计，以及仪表盘汇总展示
+ [新增] API Token、模型列表、平台分组和节点状态的同步与管理
+ [新增] PushPlus、企业微信、钉钉、飞书、SMTP 和微信公众号通知渠道
+ [新增] 定时签到全局汇总通知及账号级健康告警渠道绑定
+ [新增] 系统设置、数据备份导入导出、审计日志和系统日志管理
+ [新增] SSE 实时刷新、顶部通知中心、全局提示、命令面板和键盘快捷操作

### 部署与版本更新

+ [新增] Docker 单容器部署，由后端直接提供前端构建产物，并通过 Docker Compose 启动应用与 Watchtower
+ [新增] GitHub Actions 自动构建并发布 GHCR 镜像，推送版本 tag 后自动创建 GitHub Release
+ [新增] 侧边栏和设置页展示当前版本、检查最新正式版本及一键“更新并重启”
+ [优化] 使用 Watchtower scope 隔离本项目，固定 Docker API 版本，并仅在 Compose 内部网络开放更新接口
+ [优化] 仅在对应镜像成功发布后创建正式 Release，客户端以正式 Release 判断是否存在新版本
+ [修复] 更新接口随机等待 15-30 秒完成准备后再返回成功，避免立即返回结果或容器重建导致 Cloudflare 502 误报
+ [优化] 前端在准备期间展示更新进度，收到成功响应后固定倒计时 10 秒，等待服务器重启完成并自动刷新页面
+ [修复] 修正版本信息弹窗中的“更新并重启”确认和状态展示流程

### 交互与兼容

+ [优化] 平台地址在平台、账号、签到记录、账号详情和仪表盘中可直接访问
+ [优化] 版本号统一读取仓库根目录 `VERSION` 文件，并在登录后立即加载展示
+ [优化] 侧边栏版本入口可在任意页面打开版本与更新弹窗，有新版本时显示提示
+ [兼容] 旧账号的 `global` 代理模式在启动迁移或备份导入时自动转换为 `direct`
