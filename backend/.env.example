# 环境变量配置模板
# 使用说明：
# 1. 本地开发：使用 .env.local （已预配置）
# 2. 生产环境：使用 .env.production （已预配置）
# 3. 自定义配置：复制为 .env 并修改

# ========== 环境相关配置（需要根据环境修改）==========

ENVIRONMENT=development

# 应用配置
APP_NAME=AnyRouter Admin
APP_VERSION=1.1.0
DEBUG=true

# 数据库配置
DATABASE_URL=sqlite:///./data/anyrouter.db

# 日志配置
LOG_LEVEL=DEBUG
LOG_FORMAT=text
LOG_DIR=./logs
LOG_MAX_SIZE=10
LOG_BACKUP_COUNT=30

# 默认管理员
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123

# ========== 不变配置（所有环境保持相同，已在 config.py 提供默认值）==========
# 如需修改，请取消注释并修改

# AnyRouter API
# ANYROUTER_BASE_URL=https://anyrouter.top
# ANYROUTER_USER_API=/api/user/self
# ANYROUTER_SIGN_API=/api/user/sign_in
# ANYROUTER_CONSOLE_URL=/console
# ANYROUTER_MODELS_API=/api/user/models
# ANYROUTER_GROUPS_API=/api/user/self/groups
# ANYROUTER_TOKEN_API=/api/token/
# ANYROUTER_STATUS_API=/api/status

# JWT 配置
# JWT_SECRET_KEY=anyrouter-admin-secret-key-change-in-production
# JWT_ALGORITHM=HS256
# JWT_EXPIRE_HOURS=87600

# 请求配置
# REQUEST_TIMEOUT=30
# RETRY_TIMES=3
# RETRY_INTERVAL=3
