# 生产环境配置示例
# 复制或重命名为 .env.production 使用
# ⚠️ 重要：修改敏感信息后再使用！

ENVIRONMENT=production

# 应用配置
APP_NAME=AnyRouter Admin
APP_VERSION=1.1.0
DEBUG=false

# 数据库配置（生产环境）
# 建议使用 PostgreSQL 或 MySQL
DATABASE_URL=sqlite:///./data/anyrouter.db

# 日志配置（生产环境）
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_DIR=/www/wwwroot/anyrouter/backend/logs

# 默认管理员（⚠️ 生产用 - 务必修改）
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=change-me-in-production

# 说明：以下配置在所有环境中保持不变，已从 config.py 中提供默认值
# - AnyRouter API 配置（ANYROUTER_*）
# - JWT 配置（JWT_SECRET_KEY、JWT_ALGORITHM、JWT_EXPIRE_HOURS）
# - 请求配置（REQUEST_TIMEOUT、RETRY_TIMES、RETRY_INTERVAL）
# 如需修改这些配置，请在此文件中添加对应变量

# 高级配置（如需修改不变配置项，请取消注释并修改）
# REQUEST_TIMEOUT=30
# RETRY_TIMES=3
# RETRY_INTERVAL=3
# JWT_SECRET_KEY=your-super-secret-key-12345
# ANYROUTER_BASE_URL=https://anyrouter.top
